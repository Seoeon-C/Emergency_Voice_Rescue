# Emergency_Voice_Rescue

### 🚀 시작 가이드 (팀원 필수 확인)
레포지토리 초대 수락 후, 아래 명령어를 순서대로 터미널에 입력하여 개발을 시작해주세요.

```bash
# 1. 저장소 복제하기
git clone <레포주소>

# 2. dev 브랜치로 이동 및 최신화
git checkout dev
git pull origin dev

# 3. 본인의 기능 개발용 브랜치 생성 (기능이름 예시: feat/voice-model)
git checkout -b 기능이름
